/**
 * THE MESSAGE COMPONENT
 * --------------------------
 * In the "src" folder create "Message.tsx" file
 * 
 * The extension of typeScript files should be ".ts" or ".tsx". ".ts" is used for ordinary typeScript files while ".tsx" is ued for react-component-files. 
 * 
 * There are two ways to create a react component, we can use a Javasript-class or a function, using Javascript-function is recommended because is more concise and easier to write.
 * 
 * This component will be used in the App-component.
 */
function Message() 
{
    return <h1>Hello World</h1>

}
export default Message;